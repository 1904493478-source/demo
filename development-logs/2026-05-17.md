# Development Log - 2026-05-17

## Manual Completed Items

- Fixed the private-chat AI recognition ripple visibility issue: the source bubble now reserves an 18px chat-background feedback bed below the bubble, with a wider/longer green ripple that fades out instead of hiding behind the bubble.
- Refined the V0.2 Slice 4A recognition feedback to match the product intent: the green ripple is now a separate chat-background layer at the bottom of the source bubble, the bubble itself keeps its normal surface color, and the ripple fades out completely after the animation.
- Removed the top quick-navigation strip from the arrangements page and kept the calendar overview as the main top anchor.
- Softened bottom-sheet enter/exit motion so create, detail, and edit sheets no longer pop abruptly.
- Changed state actions to long-press reveal: `今天 / 近期 -> 以后再说`、`以后再说 -> 今天就说！`、`已完成 -> 还没完`.
- Added left-swipe delete reveal on arrangement cards and kept the existing secondary confirmation before actual deletion.
- Kept edit-sheet delete intact and verified the updated interaction flow with focused tests, full tests, and `pnpm verify:answer`.
- Finalized the next interaction redesign spec: long press will open a centered single-action mini dialog, while left swipe keeps delete but gets calmer progressive motion and clearer settle states.
- Implemented the first slice of that redesign: long press now opens a centered single-action mini dialog instead of revealing inline row actions.
- Implemented the second slice of that redesign: left swipe now follows the finger, progressively reveals a delete lane, snaps back on short swipes, settles into a delete-ready state on committed swipes, and closes the centered long-press dialog when swipe begins.
- Rebuilt the arrangements interaction slice into clean UTF-8 components, fixing broken source text and restoring stable compile/test behavior.
- Changed each arrangement row to slide as one block, moved the complete button to the right edge inside the same moving row, and simplified the delete confirmation to a single secondary action plus confirm.
- Added a durable UTF-8 encoding workflow: `pnpm check:encoding`, `docs/development/text-encoding-standard.md`, and `verify:answer` integration now distinguish real file damage from terminal mojibake.
- Fixed the duplicate left-swipe delete affordance by keeping one actionable delete button anchored in the right-side lane and moving tests to ASCII `data-testid` selectors.
- Fixed arrangement card text clipping at the top-left by adding safe inner spacing to the whole sliding row instead of pushing only the text area.
- Reviewed the remaining V0.1 scope and identified a small stabilization backlog: empty-state persistence, time-field clarity, gesture reset behavior, action undo, accessibility focus handling, and unused quick-nav cleanup.
- Completed the six-item V0.1.1 stabilization pass: empty-list persistence, dynamic time fields, undo toast, gesture auto-collapse, bottom-sheet focus/Escape handling, and unused quick-nav cleanup.
- Added `docs/development/v0.2-ai-arrangements-plan.md` for the next AI recognition slice: user-owned API setup, self-message recognition, private-chat breakfast recognition, and confirmation-before-save.
- Completed V0.2 Slice 1: added the AI settings entry, local API configuration state, local completeness test feedback, saved enabled/configured status, and kept manual arrangement creation independent from AI setup.
- Completed V0.2 Slice 2: added the AI candidate arrangement data model, local candidate persistence, pending candidate preview cards, confirm/edit/ignore actions, and a small local demo candidate trigger without making any real model request.
- Completed V0.2 Slice 3: added a self-message recognition card, a local rule-based recognizer for messages like `后天去一趟医院`, candidate-only preview behavior, symbolic-message fallback to manual creation, and green complete-button styling.
- Removed the redundant complete button from completed arrangements while keeping long-press `还没完` as the recovery path.
- Completed V0.2 Slice 4A: added implicit private-chat recognition for `明天帮我带早餐 / 好的`, writes only an AI candidate, avoids explicit recognition copy in chat, and shows a subtle green ripple / wash feedback inside the triggering message bubble.
- Fixed the V0.2 Slice 4A recognition gap reported in browser: existing accepted private-chat history is now backfilled, arrangements refresh when chat recognition stores a candidate, and handled candidates are not revived by later scans.
- Fixed the remaining V0.2 Slice 4A feedback gap: when an AI candidate already exists, the chat now backfills `data-ai-recognition` onto the matching source bubble, and the bubble uses a clearer internal green wash, halo, ripple, and inset outline. Verified in a real Edge session with `明天帮我带早餐`.

- Confirmed the latest recognition feedback implementation: durable AI recognition state is separate from the temporary 2.4s chat-background glow/ripple, and this turn only performed focused verification/logging per the user's direct-work instruction.
- Started V0.2 Slice 5 experience closure with a small candidate-preview improvement: AI candidate cards now show extracted time before confirmation, or `待补时间` when time is missing.
- Continued V0.2 Slice 5 with a focused candidate-preview improvement: AI candidates without extracted time now label their edit action as `补时间` and open the existing candidate edit sheet.
- Added a quiet pending-candidate count badge to the bottom `安排` tab so implicit chat recognition can surface without modal interruptions.

## Manual Todo Items

- Ask for user review on the adjusted ripple strength after checking the live page; reduce opacity slightly only if it now feels too strong.
- Recheck the visual strength and timing of the chat-background ripple on real mobile widths after user review, especially whether the 1.4s fade feels too slow or too subtle.
- Review the current delete-lane visual weight on real device widths and decide whether the soft danger tint should be reduced further.
- Tune the visual weight and spacing of the rebuilt right-side complete button after real-device review.
- Continue the next small interaction slice only after validating the rebuilt arrangements page in-browser.
- Use `pnpm check:encoding` as the first step for any future old-encoding or garbled-text report.
- Recheck the adjusted row spacing on narrow mobile widths after the next visual pass.
- Start V0.2 with AI settings and the candidate-arrangement data model before connecting any real model call.
- Keep V0.2 scoped to self-message and one private-chat recognition path until the confirmation flow is verified.
- Next V0.2 slice: add the candidate-arrangement data model and preview state before making any real model request.
- Next V0.2 slice: replace the local demo trigger with a self-message input/recognition path that creates candidates, still requiring confirmation before saving.
- Next V0.2 slice: tighten the AI candidate refresh path and decide whether chat recognition should surface a tiny candidate count badge on the arrangements tab.
- Verify the implicit private-chat recognition in both paths: a fresh reply flow and an already-existing `明天帮我带早餐 / 好的` conversation after refresh.
- Recheck the visual strength of the green recognition wash with the product eye: it is now intentionally visible enough to validate, but can be softened after user review if it feels too strong.

## Notes and Risks

- Record blockers, product questions, and verification notes here.

<!-- AUTO:START -->
## Auto Snapshot

Generated at 2026-05-17 23:37:37 CST (+0800).

### Git Status
- `M package.json`
- ` M pnpm-lock.yaml`
- ` M src/App.tsx`
- ` M src/components/ui/modal.tsx`
- ` M src/data/aiConversationLog.ts`
- ` M src/pages/Home.tsx`
- ` M src/styles/globals.css`
- ` M vite.config.ts`
- `?? .editorconfig`
- `?? .gitattributes`
- `?? .superpowers/`
- `?? development-logs/`
- `?? "docs/codex-logs/candidate-\346\235\216\345\260\217\346\272\252-pc-20260316jvyv-administrator-20260516T081402-f548a55b.md"`
- `?? docs/development/`
- `?? scripts/check-text-encoding.mjs`
- `?? scripts/update-dev-log.mjs`
- `?? src/App.test.tsx`
- `?? src/features/`
- `?? vite-dev.log`

### Commits Today
- No commits recorded today

### Completed Plan Steps
- Step 1: Add Vitest dependencies
- Step 2: Add scripts
- Step 3: Configure Vitest
- Step 4: Write the first failing state test
- Step 5: Run the test and confirm RED
- Step 1: Define types
- Step 2: Implement minimal status derivation
- Step 3: Run the test and confirm GREEN
- Step 4: Add tests for completion and “以后再说”
- Step 5: Run and confirm RED
- Step 6: Implement completion and postpone
- Step 7: Run tests
- ...and 27 more item(s)

### Open Plan Steps
- No open plan steps found

### Verification Reminder
- Run `pnpm verify:answer` before marking a development slice complete.
<!-- AUTO:END -->
