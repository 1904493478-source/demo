# Development Log - 2026-05-18

## Manual Completed Items

- Added a quiet pending-candidate count badge to the bottom `安排` tab for implicit AI chat recognition.
- Stabilized time-sensitive arrangements tests by fixing their system time before checking the `今天` group.
- Broadened private-chat AI recognition from the fixed breakfast phrase to a low-cost responsibility-transfer frame: request cue, executable objective, and acceptance or commitment reply.
- Added coverage for general requests, reordered wording, middle request cues, multi-item merge into one candidate, refusals, and follow-up questions.
- Connected real AI recognition through the existing user-provided OpenAI-compatible API settings.
- Added an `AiArrangementClient` for `/chat/completions` requests and JSON candidate normalization.
- Wired self-message recognition to try the real model first, then fall back to local rules.
- Wired new private-chat replies to refine pending candidates with the real model while keeping historical backfill local and token-free.
- Kept all AI results candidate-only: no formal arrangement is created until the user confirms or edits and saves.
- Closed V0.2 by changing the AI settings connection test from local field validation to a real OpenAI-compatible `chat/completions` request.
- Updated AI settings copy to explain Chat Completions, current-browser API Key storage, token usage, and candidate-only behavior.
- Started V0.3 with the smallest group-chat recognition slice: when a group member explicitly @mentions the current user with a request and the user accepts, the app creates a pending `groupChat` AI candidate and marks the source bubble with quiet recognition feedback.
- Added V0.3 planning notes that keep group-chat recognition candidate-only and defer broad group semantics, sync, merge, and AI API refinement to later slices.
- Fixed the first V0.3 runtime gap: group-chat recognition now falls back to the known candidate name `李小溪` when the Demo user profile has not been initialized, so `@李小溪` can still create a candidate and show feedback.
- Improved group-chat recognition feedback visibility: after a candidate is created from a group reply, the chat view now keeps focus on the original recognized request bubble instead of immediately targeting the new reply.
- Added V0.3 Slice 2: new group-chat replies now call the configured OpenAI-compatible API to refine the pending `groupChat` candidate, while historical group backfill remains local and token-free.
- Added V0.3 candidate aggregation: pending candidates with the same topic merge into one card, keep all original source contexts, show per-source confidence, and flag conflicting times.
- Refined aggregation into scene-based merging: same topic still merges, and same place within a 3-hour window now merges while far-apart same-place items stay separate.
- Closed the current V0.3 stage after full verification, documenting the next-stage entry points: intelligent completion first, then no-@ group relevance, with formal arrangement merging deferred.
- Added the minimal intelligent completion loop: when a later chat reply clearly says an arrangement has happened, the matched arrangement moves to `可能已完成` instead of being silently completed.
- Added explicit completion confirmation UI: possible-completion details show a quiet green hint, `确认完成`, and `还没完` so the user controls the final state.
- Added conservative no-@ group relevance: open group requests such as “谁可以帮我…” only create a low-confidence candidate after the current user clearly replies with a first-person commitment.
- Fixed arrangement list deletion coverage across all groups, including completed arrangements and older completed items after expansion.
- Changed the completed group to show only the 3 most recently completed arrangements by default, with a small expand/collapse control for the rest.
- Changed the calendar overview to default to today's arrangements only, and to switch the list below when a calendar day is tapped.
- Fixed duplicate arrangement ids in storage/state so repeated AI confirmations no longer create stacked identical calendar/list cards or linked swipe lanes.
- Expanded the completed-card swipe hit area so swiping the visible card surface also reveals the delete action.
- Closed the current V0.4 stabilization slice and documented the stable boundary plus next-stage entry points.
- Stabilized the App-level arrangement navigation test by giving the bottom Arrangements tab a dedicated test target, avoiding same-label button ambiguity after returning from chat.
- Started V0.5 Slice 1: added AI-assisted possible-completion fallback after local matching misses, while keeping completion user-confirmed.
- Added arrangement execution capability layers in detail: user-only, AI-assist, and AI-auto with authorization/confirmation/rollback safety copy.
- Preserved optional AI execution capability fields in arrangement storage while keeping older stored arrangements compatible.
- Completed V0.5 Slice 2: AI-assist actions in arrangement detail now generate an in-place draft for preparing materials, drafting replies, or route planning.
- Added OpenAI-compatible assist draft generation with local fallback templates, while keeping every assist result draft-only with no status change or external execution.
- Expanded intelligent-completion recognition for follow-up phrases such as "资料已经拿回来了", "取回来了", and "带回来了".
- Replaced the visible `可能已完成` list/detail copy with a lightweight pulsing green complete button, so completion suggestions do not add a bulky page section or move the arrangement away from its original group.
- Added immediate visible feedback for AI-assist actions: detail now shows "正在生成草稿" while the model is running and falls back to local drafts if the request is slow or fails.
- Added the final first-run guide for the `安排` module, summarizing manual creation, AI candidates, completion confirmation, "以后再说", and AI assistance the first time the user opens the tab.

## Manual Todo Items

- Refine no-@ group relevance with better confidence explanation and candidate filters.
- Formal arrangement merge should stay deferred until split/undo behavior is designed.
- Decide whether AI-assist drafts should become copyable, editable, or persist temporarily in detail history.
- Future AI-auto execution still needs real permission scopes, confirmation UI, execution logs, and rollback behavior before connecting external services.

## Notes and Risks

- The current private-chat recognizer is still a local V0.2 heuristic; ambiguous natural language should remain candidate-only and user-confirmed.

<!-- AUTO:START -->
## Auto Snapshot

Generated at 2026-05-18 06:18:49 CST (+0800).

### Git Status
- `M "docs/codex-logs/candidate-\346\235\216\345\260\217\346\272\252-pc-20260316jvyv-administrator-20260516T081402-f548a55b.md"`
- ` M src/data/aiConversationLog.ts`

### Commits Today
- c37bdd3 Merge personal repository initial commit
- bd4fb3d Document arrangements demo
- a38dbdb Initial commit
- d396007 Implement arrangements module

### Completed Plan Steps
- Step 1: Add Vitest dependencies
- Step 2: Add scripts
- Step 3: Configure Vitest
- Step 4: Write the first failing state test
- Step 5: Run the test and confirm RED
- Step 1: Define types
- Step 2: Implement minimal status derivation
- Step 3: Run the test and confirm GREEN
- Step 4: Add tests for completion and “以后再说”
- Step 5: Run and confirm RED
- Step 6: Implement completion and postpone
- Step 7: Run tests
- ...and 27 more item(s)

### Open Plan Steps
- No open plan steps found

### Verification Reminder
- Run `pnpm verify:answer` before marking a development slice complete.
<!-- AUTO:END -->
